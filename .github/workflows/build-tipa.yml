name: 构建 Bootstrap TIPA (RootHide)
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: 拉取仓库代码（递归子模块）
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: 安装 Theos 到用户目录（解决权限问题）
        run: |
          # 清理旧 Theos（用户目录可写）
          rm -rf $HOME/theos
          # 克隆 RootHide Theos 到用户目录（无需 sudo）
          git clone --recursive https://github.com/roothide/theos.git $HOME/theos
          # 验证 Theos 核心文件
          if [ ! -f "$HOME/theos/makefiles/common.mk" ]; then
            echo "Theos 安装失败：common.mk 缺失"
            exit 1
          fi
          if [ ! -f "$HOME/theos/makefiles/library.mk" ]; then
            echo "Theos 安装失败：library.mk 缺失"
            exit 1
          fi
          # 配置全局环境变量（绝对路径）
          echo "THEOS=$HOME/theos" >> $GITHUB_ENV
          echo "PATH=$HOME/theos/bin:$PATH" >> $GITHUB_ENV
          echo "THEOS_MAKE_PATH=$HOME/theos/makefiles" >> $GITHUB_ENV

      - name: 安装 ldid + 配置 Xcode
        run: |
          # 安装兼容版 ldid（无需 sudo 也可写入 /usr/local/bin）
          brew uninstall --force ldid || true
          curl -LO https://github.com/ProcursusTeam/ldid/releases/latest/download/ldid-macos-x86_64
          chmod +x ldid-macos-x86_64
          mv ldid-macos-x86_64 $HOME/theos/bin/ldid
          chmod +x $HOME/theos/bin/ldid
          # 指定 Xcode 路径
          sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer
          # 验证依赖
          $HOME/theos/bin/ldid --version || echo "ldid 已安装"
          xcodebuild -version

      - name: 硬编码 Theos 路径到 Makefile（关键）
        run: |
          cd $GITHUB_WORKSPACE
          # 备份原 Makefile
          cp Makefile Makefile.bak
          # 替换所有 Theos 路径为用户目录绝对路径
          sed -i '' "s|\$(THEOS)/makefiles/common.mk|$HOME/theos/makefiles/common.mk|g" Makefile
          sed -i '' "s|\$(THEOS_MAKE_PATH)/xcodeproj.mk|$HOME/theos/makefiles/xcodeproj.mk|g" Makefile
          # 修复 XPC 模块重定义
          sed -i '' 's/module XPC/module CustomXPC/g' basebin/common/xpc/module.modulemap || true
          # 创建空目录兜底（避免路径缺失）
          mkdir -p Bootstrap/basebin/common/{include,lib}

      - name: 编译并打包 TIPA
        run: |
          cd $GITHUB_WORKSPACE
          # 清理旧产物
          make clean
          # 强制指定 Theos 路径编译（绝对路径）
          THEOS=$HOME/theos make package \
            ARCHS=arm64 \
            TARGET=iphone:17.5:15.0 \
            Bootstrap_XCODEFLAGS="\
              IPHONEOS_DEPLOYMENT_TARGET=15.0 \
              CODE_SIGN_IDENTITY='' \
              AD_HOC_CODE_SIGNING_ALLOWED=YES \
              CODE_SIGNING_ALLOWED=NO \
              EXCLUDED_SOURCE_FILE_NAMES='Makefile' \
              USER_HEADER_SEARCH_PATHS='$(PROJECT_DIR)/basebin/common $(PROJECT_DIR)/Bootstrap/basebin/common' \
            "

      - name: 上传 TIPA 产物
        uses: actions/upload-artifact@v4
        with:
          name: Bootstrap-TIPA
          path: ${{ github.workspace }}/packages/Bootstrap.tipa
          retention-days: 90
